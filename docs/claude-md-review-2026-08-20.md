# CLAUDE.md review loop log

`CLAUDE.md` is loaded into every session on every turn, so its review history
is kept here rather than in the document — a table appended to forever is a
permanent context cost carried by every future session against an audit trail
almost none of them will read. The document carries a one-line pointer at this
file and nothing else.

Genre: **standard** (pinned). Cap 3. Questions asked: Q1, Q2, Q3 — Q4 is not
asked of a standard.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Outcome |
|------|------|-------|----|----|----|---------|
| 1 | 2026-08-20 | 3, cold — packet carried FUNDING.yml, the CMake generation block, the generated header, the size bar, the tile and grid code, the Help menu, `donate.*`, the new uitest blocks, and the measured per-game minimum sizes | 4 | 1 | 1 | **Six verified, six fixed; two open questions resolved clean.** First gate ever run on this file, triggered by GHUB-0041 and GHUB-0042. **All three lanes independently found the same two defects**, which is the strongest signal in the run. The document said *"the fourteen per-game passes that read the switch have not started yet"* while two of them had shipped and are described at length in its own Traps section — an implementer would have written Canasta's pass a second time, duplicating the very pre-clamp geometry restore the Traps warn about. And its opening inventory listed **Klondike**, which is not a registered game name: `hubwindow.cpp` registers `Solitaire` with `Klondike` as the blurb, so `--game klondike` warns and opens the tile grid. **Two lanes found this session's own collateral**: the legibility bullet still claimed to be *"the one app-wide persisted preference"* six lines above a new bullet introducing `donate/ask`. **The best single-lane finding is about CI**: `for every push` is false — `ci.yml` runs on push to `master` and on pull requests only, so branch work gets no Windows leg at all, and this document names CI as the only place MSVC is exercised. Also fixed: the pre-push docs-only arm exempts `.gitignore` too, which the enumerated list omitted. **One Q3 was on text added this session** — *"must never be typed into a source file"* was unstatable, because `CMakeLists.txt` legitimately holds the platform URL stems and is where a new key's rule goes; two builders adding Ko-fi would have settled it differently. **Phase 4a step 3 caught the fix being wrong**: the replacement asserted that routing Ko-fi through `custom:` *"builds and works"*, and running it showed the build refuses a two-entry `custom:` list outright. Two lane open questions resolved clean — the `--game` path consuming an owed prompt is deliberate and recorded in the code, and the per-game card widths are properties of each view's own minimum, untouched by the hub's. |
| 2 | 2026-08-20 | 3, cold — identical brief, packet and scrubbed copy rebuilt from disk | 4 | 0 | 1 | **Five verified, five fixed.** **All three lanes independently found the same defect, and it is the run's most consequential**: § Releasing says *"three edits and a tag"* and lists **two** — `README.md`'s `Current version` line is the third, `.claude/bump.json` enumerates it with a `post_check` that fails on drift, and `release.yml`'s `verify` job never looks at the README. A conformer following the list publishes a release page still advertising the previous version, and the paragraph below assures them the workflow would have stopped it. The section now names four numbered steps and points at `bump.json` as the file list of record. **One finding was loop 1's own collateral** — that loop's fix said `donate/ask` was the only other app-wide key, and `donate/launches` is a third; a settings reset built from it clears the switch, leaves the counter at 149 and fires the prompt on the very next start. **Two landed on text this session wrote**: the size bar was described as binding *"a new game"* when the check binds every game in both legibility states — an implementer doing one of the twelve remaining GHUB-0017 passes could design a minimum above the bar believing it did not apply, and Canasta's 900 leaves 60 pixels of headroom — and the donate bullet described *"the every-150th-launch prompt"* without the `--game` exception, which reads as an off-by-one and invites deleting a deliberate guard. **The one pre-existing Q1 was a measurement that had simply gone stale**: *"the binary is ~800 KB with the audio and ~425 KB without, which is the quickest check"* — measured, it is 1.61 MB, so the stated check gives no signal either way. Replaced with the delta (17 files, ~380 KB, matching the compiled resource object) and a pointer at the uitest assertion that actually checks it. One out-of-scope correction next door: GHUB-0017's ROADMAP headline still said thirteen games remained; the headline is a store column and cannot be amended, so the count went in as an annotation. |
| 3 | 2026-08-20 | 3, cold — identical brief, packet and scrubbed copy rebuilt from disk | 3 | 0 | 1 | **Four verified, four fixed. Cap reached (3 for a standard); the run files its tail and exits.** **The run's best finding is one no reader could have got from the document**: two lanes independently opened `tests/uitest.cpp` and found the window-size check reduces to the single page with the largest **AREA**, then measures only that page against the bar — so a game at 1100x300 never wins the area contest against Canasta's 900x740 and its 1100 width is never compared to the 960 bar at all. The doc's claim was the stronger, intended one, so **the fix went to the code**: width and height are now tracked separately, with the page that owns each named in the printout. Today's data does not distinguish the two forms — Canasta is both widest and tallest — so this is a guard for the twelve outstanding per-game legibility passes rather than a live bug, and that is stated in the test's own comment. **All three lanes found the settings enumeration**, which loop 1 introduced and loop 2 widened once already: `window/geometry/<page>` and `saved/<game>` are written outside any game's group too, so a reset built from the sentence would leave stale geometry and resume games it was meant to forget. Now stated as four families with `geometryKey()`/`saveKey()` named as their home, and verified by enumerating every settings key in `src/`. **One pre-existing Q3 was a sentence with no subject** — *"The matching one-per-flight, so several cards arriving at once each get their own slot"* — pointing at `CanastaView::m_consumed` without naming it; an implementer would build `suppressed()` as a plain "is this card in flight?" test, and two identical cards arriving together would then hide both destination copies. Routine here rather than exotic, since Canasta shuffles two packs and `Card::deck` sits outside `operator==`. **One correction was the orchestrator's own**: loop 2's fix asserted GitHub allows up to four `custom:` funding URLs, which is an external-platform fact no lane and no local command can settle — softened to what is checkable. **Cap character: three of the four findings landed on text this run wrote, which is nominally a violent cap — but read it carefully.** Two of the three were widening corrections on documentation of code this session had just written, not repairs of repairs, and every thread terminated: the bar-scope thread by moving the fix into the code, the settings thread by enumerating the keys mechanically rather than from recall. The document is 616 lines, comfortably inside the range where two cold reads reach all of it, so the size signal argues against a split. **Route: ship.** |
| 3 | 2026-08-20 | 3, cold — new run, trigger GHUB-0071..0073; packet carried `gameview.h`/`.cpp` whole, the caption declarations, `kFaceMinWidth`, Canasta's `applyLegibility`, Pinball's activate/deactivate, both new uitest blocks, the override census, GHUB-0017 §1/§3, `bump.json` and the CMake split | 4 | 1 | 1 | **Six verified, six fixed; one finding dismissed as MY packet's fault; two open questions resolved clean.** **All three lanes independently found the same three defects**, and the most consequential is the oldest: **the architecture section opened by claiming every game is a rules core plus a view**, which is false of six of the fourteen — Klondike, Spider, FreeCell, Pyramid, Snake and 2048 have no core at all, so `gameshub_selftest` cannot link their rules. A conformer told to add a rules check for one of them writes it into the self-test and it will not link, and the section gave no hint. GHUB-0066 is the open item and is now cited there; its own body quotes the sentence that just changed, so it was annotated rather than edited. Same defect one section later: *"each core's `restore()`"* named a 2048 core that has never existed. **The Q2 was this session's own collateral** — CLAUDE.md said all fourteen passes had shipped while `docs/specs/GHUB-0017-legibility-switch.md`, which the same sentence names as the contract, still said twelve games ignored the switch; a conformer sent to the named contract would have built twelve passes that already exist. The spec's status block was corrected next door (an amendment recording what was built, which rule 14 exempts from re-arming the gate). **The Q3 came from one lane and is the sharpest finding of the run**: § Releasing warned that a forgotten README bump *"is caught only by `bump.json`'s `post_check`"* while its own numbered procedure never ran it — and a repo-wide search confirms nothing runs it, not a hook, not `local-ci.sh`, not the workflow. It is now step 4, executed and its output pasted; the intro's step count was corrected in the same sweep, which was collateral I introduced by adding the step. **One finding was dismissed and the fault was the packet's**: all three lanes reported that the deactivate assertion does not exist, quoting the block's own *"REPORTED and left out rather than asserted about"* comment — the assertion is at `tests/uitest.cpp:557` and my `head -105` window cut it. Two of the three lanes flagged the gap as an open question rather than asserting it, which is the lanes working. **Never assert an absence from a truncated window** — the same lesson this file's loop 4 learned from an elided quote, in a new costume. **One open question was worth more than most findings**: a lane asked whether the card-width figures were measured before the caption band existed. They were — Pyramid had moved 68.6 → 52.1 and the trap still said 68.6. The figures are now readings the suite prints every run rather than history. The other resolved clean: `assets/sounds` is 17 files and 387,062 bytes, so *"about 380 KB"* is right and the packet's `du -sh` 416K was block usage, another packet artefact. **Collateral outside the document: two** — the GHUB-0017 spec status block, and GHUB-0066's annotation. **Cap not reached** (loop 3 of 3 for the document, loop 1 of this run). The document is 704 lines, still inside the range two cold reads reach. |
| 4 | 2026-08-20 | 3, cold — identical brief, packet rebuilt from disk with the uitest blocks quoted COMPLETE (loop 3's gap) | 2 | 2 | 3 | **Five verified, five fixed — two of them in code rather than in the document; four open questions resolved clean.** **All three lanes independently found the same defect**, and it is a rule that had been wrong since long before this session: § Architecture states the core/view split as a purely technical test — *"anything that pulls in QtWidgets belongs in the view half"* — while `CMakeLists.txt` applies a second, semantic one. `legibility.cpp`, `scores.cpp` and `donate.cpp` are all QtCore-only and all sit in the view half on purpose, `legibility.cpp` carrying the reason inline. A conformer adding a preference store reads the stated test, puts it in `GAME_CORE_SOURCES`, and it links and passes — the breach is invisible because the only stated criterion is one it does not trip. **Two lanes found the sharpest Q3**: `applyLegibility` is never called at construction, `gameview.h` says so, and this document said nothing about it — and the new check cannot catch the omission, because it builds every game with the switch OFF. Resolved a lane's code-side question rather than assuming: **no view reads the switch in its constructor and none needs to**, because every one reads it live at the point of use; that is now stated as the rule, with the caching case named. **Two lanes found the Q2**: § Architecture promised the board *"shrinks by exactly what the sentence takes rather than being covered by it"* while the trap three hundred lines later says the band is fixed, capped at 22%, and that a caption may overlap under the cap — an implementer would have read overlap as a bug and rebuilt the uncapped, platform-font-derived band the trap exists to prevent. **Two findings landed in code and are the run's best return.** A lane noticed the every-game block's own header comment still said a restless game *"is REPORTED and left out rather than asserted about"* while the body asserts `restless.isEmpty()` — **that stale comment is precisely what misled all three of loop 3's lanes into reporting the assertion missing**, so the packet gap and the comment were the same defect wearing two hats. And a lane found the `deactivate()` assertion was asked of thirteen games, not fourteen: the Sudoku skip sat at the top of the loop, excusing it from the stop-on-leave check as well as from the picture comparison. The skip moved below the stillness probe. **Measured rather than claimed**: deleting Sudoku's own `m_tick->stop()` still reddens nothing, because its clock is idle on a fresh board — so the assertion catches a game actually IN MOTION, which is Pinball alone today, and the document now says that instead of implying whole-suite coverage. **Two lane open questions were worth more than most findings** and produced the third fix: Hearts draws a caption but reserves no band (it uses the gap between the trick and the hand), and Pinball draws no caption at all (it grows its backglass), so *"a pass"* had three shapes and the document described one. **Collateral: none outside the document.** **Also this loop, outside the gate:** the suite was run eight times after a one-in-six flake appeared in an assertion added this session — *"no cards in flight"* checked at a fixed instant, when the computers keep playing and every AI move launches more; replaced with a bounded wait for the falling edge, then 8/8 clean. **Cap reached** (loop 4, and the cap for a standard is 3 per run — this is loop 2 of this run, so the cap does not bind). Of this loop's five verified findings, **two landed on text this run wrote** and three were pre-existing, which is a calm loop rather than an oscillating one. Document 736 lines. **Route: ship.** |
| 5 | 2026-08-20 | 3, cold — identical brief, packet rebuilt from disk | 4 | 2 | 2 | **Eight verified, eight fixed; two more defects fixed in code; four open questions resolved. CAP REACHED (3 loops for a standard) — the run files its tail and ships.** **All three lanes independently found the same defect for the third loop running**: the caption taxonomy added in loop 4 said *"two of the fourteen are not"* a caption and then accounted for twelve games — Canasta and Sudoku, the two whose passes shipped first, were missing from it entirely. A maintainer auditing it would have concluded they had no pass and given Canasta a caption band, which is the one arithmetic the document elsewhere says must never clamp: its melds clear `kFaceMinWidth` by 0.4 px. There are four shapes, not three. **Two lanes found the Q2 that came with it**: the card-width figures were labelled *"with the switch on, so the caption band is already subtracted"*, and Hearts and Canasta reserve no band at all — so two of the six figures presented as band-inclusive carry no band cost, and raising the cap would silently invalidate them. **A lane found the exclusivity claim false**: *"Pinball … the one game that already spoke on its own surface"* against the document's own account of Canasta spelling out the last discard under the centre of the table. **And another found the live-read claim too strong** — seven games never name `Legibility` at all and are correct as they stand, inheriting the read through `paintStatusCaption()`; as written the sentence invited a redundant call or a cached copy, which is the failure the next sentence warns about. **The best Q3 is one nothing catches**: `smallestCardWidth()` defaults to 0, `cardsKeepTheirFaces` skips a game answering 0, and its `checked >= 6` floor is already met — so a fifteenth card game that forgets the override ships drawing faceless cards, with no test and, until now, no sentence. **A lane open question turned into the run's most valuable fix and it was in the code, not the document.** Asked whether the stop-on-leave assertion really reaches only Pinball, the answer was worse than the claim: **Snake and Hearts each own a `QTimer` and neither overrode `deactivate()`** — leaving Snake mid-game drove the snake into a wall, leaving Hearts finished the hand without you, and every green run of the every-game block had missed it because a pixel probe only catches a game that HAPPENS to be moving. Both fixed, Hearts with the resume path its `activate()` needed, and a new `gamesStopTheirClocks` block now asserts the rule structurally — no `QTimer` may be active after `deactivate()`, for all fourteen. **Honest gap recorded rather than hidden: removing Hearts' resume reddens nothing**, because reaching a state where its clock is due needs the pass driven first; Snake's break was seen red, Hearts' was not. **One more of this session's own defects, found while fixing:** leaving Snake's and Pinball's views running inside the new block kept their timers alive through the rest of the suite — Pinball repaints on every pump — and turned a 34-second run into one that had not finished in two minutes. Scoped, and the reason recorded in the block. **Also settled from lane questions:** a settings reset must clear every per-game group as well as the four app-wide families (`scores.cpp` writes `<game>/best_*`, Canasta keeps `canasta/house/`), and the new § Committing rule was imprecise — the subject may abbreviate `GHUB-0066..0070`, which contains no literal `GHUB-0067`, so it is the BODY that must enumerate, and every grouped commit here already does. **Cap character: of eight verified findings, five landed on text loops 3 and 4 wrote and three were pre-existing.** That is the shape § At the cap calls a run repairing its own repairs, and it is why the cap binding here is the right place to stop rather than a reason to run a fourth. **Route: ship.** Document 776 lines. |
| 6 | 2026-09-02 | 3, cold — new run, trigger GHUB-0093/GHUB-0166; packet carried `main.cpp`'s option block and `takeShot`, `gameview.h`'s hook, `dealseed.h` whole, the shot ctest cases, `canastaview.cpp`'s settings writer and `advanceForShot`, the core/view split, and a live `ctest -N` | 5 | 0 | 2 | **Seven verified, seven fixed; three open questions resolved clean.** Not one Q2. **All three lanes independently found the same defect, and this session had just been burned by it**: the one enumeration of what `ctest` runs named seven cases where nine are registered, omitting `scorepad` and `legibility` — and `legibility` is the case whose Linux-only `grep` pipeline had reddened the Windows leg six times that morning. A conformer restating `kFaceMinWidth` as a literal reads the threshold script as having no automated caller. The hand-run block was built from the same wrong map, so a session checking everything by hand skipped both. **Resolving a lane's open question produced the finding that would have prevented the morning**: § Releasing said `ci.yml` *"builds and runs both test binaries"*, and it runs `ctest` — every registered case, Python checks included. Had that been right, the Windows failure would have been predictable from this document alone. **Two lanes found the ladder rung**: `canastaLevelsDiffer()` was described as playing *"each rung against the one below it"*, which gives three comparisons for four levels, while the document lists four and reads `hard v easy` as evidence. Someone changing Hard would re-measure two rungs and be unable to account for a third moving. **One lane found a claim GHUB-0066 falsified**: 2048 was still described as having no core, with its save check *"in the view"* — `twenty48board.cpp` has been in `GAME_CORE_SOURCES` since that item shipped and the check is in it, so an implementer hardening it would have written into the view where the self-test cannot link. **Both Q3s landed on text this commit wrote, and both are about a fifteenth game.** `advanceForShot`'s settling duty was stated as a property of `--turns` rather than an obligation on whoever overrides it — `gameview.h` states it as a contract — so a Hearts override would run its turns, return true, and photograph a card mid-flight, caught by nothing since the ctest case photographs Canasta only. And `--seed`'s reach was stated as a fact about today with no obligation attached: a new game reaching for `std::random_device` drops silently out of it, and the seeded case never compares two shots so it passes either way. **Two lanes filed the same figure as a false claim and it was not one.** `dealseed.h` cited GHUB-0093's 10633-against-6416 beside this document's ~101,000 of 740,000, and the lanes read the smaller as a seeded noise floor contradicting *"byte-identical"*. Both are UNSEEDED and byte-identical is measured with `cmp`; the defect was that neither said so, next to a claim about seeding. Corrected in both places — the `dealseed.h` half is collateral outside the subject. **Also fixed from a lane question**: the scratch-config instruction never named the INI group the House keys sit under, which cost this session a wrong turn on the way to the first `--turns` shot. **Resolved clean, not counted**: the 18-full-games and 408-ladder-games figures are two different checks and both true; the `+233`/`+236` discrepancy two lanes raised is the pre- and post-GHUB-0149 baseline, not drift; `useHouse` is a real key. **All three lanes disclosed that the harness had injected a superseded copy of the subject as project instructions, and sourced their findings to the on-disk file anyway** — worth recording, because a lane that had not noticed would have reported the old text as current. |
| 7 | 2026-09-02 | **2, cold** — lane B lost to a session rate limit and not re-dispatched, so this loop bought two rolls rather than three; identical brief, packet rebuilt from disk | 1 | 0 | 1 | **Two verified, two fixed; three open questions resolved clean. Both findings landed on text loop 6 wrote**, which is the pattern this log has recorded in every multi-loop run, and **both surviving lanes found both of them independently**. **The Q1 is a count loop 6 introduced and got wrong in the direction that wastes somebody's afternoon**: it said a Windows box with the Store stub `python.exe` registers seven cases against the runner's nine, making the whole gap the two Python checks. `prepush` is inside `if(UNIX)` — its failing placeholder too — so a Windows box registers no hook test either, and the extremes are six and nine, never seven. A maintainer checking a Windows configure against the stated figure sees six and hunts for a case that was never registered there by design, which is the exact failure the CMake comment beside it exists to prevent. Fixed by naming the three cases that vary and their platform reason, and by saying to count with `ctest -N` rather than against a figure in prose — the number moves whenever a case is added, and loop 6 had just added two. **The Q3 is the sharper of the pair and is the second consecutive loop to land on `advanceForShot`.** Loop 6 turned the hook's settling duty into an obligation; this loop found the obligation states the wrong half. `gameview.h`'s contract is *synchronous, unanimated and silent*, and `CanastaView::advanceForShot` opens by saying it goes straight through the engine rather than `aiHalfTurn()`, "the presentation path: it flies cards, makes noise and sets a pause". The document said only *settle before returning*. A fifteenth game's author — Hearts and Snake both drive their AI off a `QTimer` — would loop the existing presentation path N times, clear flights at the end, and believe they had complied; no timer fires inside a synchronous call, so the engine never advances and `--shot` photographs the deal. The obligation now leads with how the turns are driven, and keeps settling as the second half. **Resolved clean, not counted**: the 18-full-games figure is `canastaFullGames`, three levels by six games, a different check from the 408-game ladder and true as written — raised in loop 6 as well, so it is now settled twice and worth a window in any future packet; `ci.yml`'s "three jobs" is right as GitHub reports them, the `build:` matrix expanding to Linux and Windows beside `sanitizers:`; and a `--shot` run does not advance the donate counter, because `takeShot` returns before `recordLaunchAndAsk()` is reached. **Cap not reached** (loop 2 of 3 for a standard). Document 960 lines. |
