# GHUB-0161 — translatable text: review loop log

The review history of `docs/specs/GHUB-0161-translatable-text.md`, kept here
per `spec-format.md` § 6 so the spec itself carries only a pointer.
`review-contract` writes one row per loop, as the loop closes.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-10 | 3, cold — genre pinned `spec`; all three lanes arrived holding the pre-split `CLAUDE.md` from session auto-load | 3 | 6 | 1 | 3 | **Thirteen findings after merging: twelve verified and fixed, one dismissed.** Fixed: § 4.4's Reversi notice could not be one sentence without changing the English or keeping a fragment (all three lanes) — now two whole sentences; § 4.5's check flagged `translate()`'s context argument (lane 1), flagged key and identity literals § 4.2 never wraps (lane 3), and let one marker hide a bare label on the same line (lanes 3 and 1) — rewritten; INV-3's `tr()` case, Snake, Pinball and the `QSettings` names were reachable by no test (lanes 1 and 3) — a marking-translator `uitest` case added; INV-4 was checked only on this item's diff (lane 1) — the check now refuses a `+` join; counted `const char*` text needs `QT_TRANSLATE_N_NOOP` (lane 2, confirmed in `qttranslation.h`); a menu mnemonic now travels with its label (lane 2's open question). Orchestrator-found: `legibility-check.py` keeps `PAIRS` as a constant, not in its header; § 8's `lrelease` reason no longer held; and two clashes this loop's own § 4.5 rewrite created (terminal-output calls unnamed; INV-1 said `exempt`). Dismissed: `uitest` does match `Undo` and `Discard`. Loop 2 dispatched. |
| 2 | 2026-09-10 | 3, cold — identical brief, packet and subject copy rebuilt from disk; all three lanes again arrived holding the pre-split `CLAUDE.md` | 0 | 0 | 1 | 2 | **Three findings after merging, all verified and fixed; none dismissed. The cap for a spec; nothing deferred.** Fixed: INV-3's test asserted the `QSettings` identity, but `uitest` runs under its own on purpose and cannot see `main()`'s (all three lanes) — that half now has no test and § 10 says so; INV-3's test let `gameNames()` stand in for the keys and never read one, so a key built from `label` passed (lanes 1 and 3) — the case now checks `window/geometry/<id>` and `saved/<id>` unmarked; the window title's `Games <version>` part was neither wrapped nor fenced (lane 2) — § 4.2 now fences it as `--version`'s spelling. **Violent cap: two of the three landed on INV-3's test clause, which loop 1 wrote**, so the review ends here and the spec routes to implementation, where INV-3's test is the clause most likely to need folding back. Share on the triggering change: `8e976ea` created the whole document, so every finding of the run falls inside it by construction. |
| impl | 2026-09-10 | none — implementation fold-back (write-spec Step 8); no reviewer dispatched | 0 | 0 | 0 | 0 | **Three clauses amended to record what the build did; no gate re-armed, since each records built behaviour.** § 4.5, INV-4 and § 10: the check refuses `+=` onto a translation call as well as `+`. § 4.4: where today's English picks its own singular (FreeCell's card count, Spider's suit count), the singular stays a whole sentence beside the `%n` form, because a lone `%n` form prints "1 cards" with no translation loaded and breaks INV-2. § 4.5: a qualified call counts by its last name part, and Qt placeholders are not letters. **INV-3's test, which this log's loop-2 routing named as the clause most likely to need folding back, was built as written**; it passed, and six mutations of the id/label split were run against it. |
